/*
  Single place to put common configuration settings.
*/

const config = {
  networkID: 12345,
  fullNodeHost: 'localhost',
  fullNodePort: 9650,
  fullNodeProtocol: 'http'
}

module.exports = config
